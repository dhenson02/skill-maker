{
  "name": "skill-maker",
  "private": true,
  "type": "module",
  "description": "Dev tooling for the Skill Maker Chrome extension (the extension itself has no build step).",
  "scripts": {
    "test": "node --test 'tests/unit/*.test.mjs'",
    "test:e2e": "node --test 'tests/e2e/*.test.mjs'",
    "test:all": "npm test && npm run test:e2e",
    "package": "node scripts/package.mjs",
    "test:package": "npm run package && EXTENSION_DIR=dist/unpacked npm run test:e2e",
    "assets": "node scripts/store-assets.mjs"
  },
  "devDependencies": {
    "jsdom": "^30.1.0",
    "jszip": "^3.10.1",
    "puppeteer": "^25.11.0"
  },
  "engines": {
    "node": ">=22"
  }
}
